#include <ParasolidDoc.h>



void ParasolidDoc :: OnOpenFile()
{
	 int npartitions;
	/*	PK_PARTITION_t *partitions = NULL;

		PK_SESSION_ask_parts( &m_nparts, &m_parts );

		if (m_nparts > 0)
		{
			PK_ENTITY_delete( m_nparts, m_parts );
			PK_MEMORY_free( m_parts );
		}

		PK_SESSION_ask_partitions( &npartitions, &partitions );
		// There should only be one partition

		PK_PARTITION_ask_geoms( *partitions, &m_ngeoms, &m_geoms );
		
		if (m_ngeoms > 0)
		{
			PK_ENTITY_delete( m_ngeoms, m_geoms );
			PK_MEMORY_free( m_geoms );
		}

		PK_MEMORY_free( partitions );
		*/
}